common:
  tags:
    - functional
    - views
  # Rendering suite: only platforms the application supports (see app/boards).
  platform_allow:
    - native_sim
    - qemu_cortex_a9

tests:
  functional.views: {}
